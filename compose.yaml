services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: anki_miner
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      - anki-miner-postgres:/var/lib/postgresql/data

volumes:
  anki-miner-postgres:
